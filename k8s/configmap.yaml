apiVersion: v1
kind: ConfigMap
metadata:
  name: ignition-config
  namespace: ignition
  labels:
    app: ignition
    app.kubernetes.io/name: ignition
    app.kubernetes.io/component: config
data:
  NODE_ENV: "production"
  APP_NAME: "Ignition AI Project Dashboard"
  APP_VERSION: "1.0.0"
